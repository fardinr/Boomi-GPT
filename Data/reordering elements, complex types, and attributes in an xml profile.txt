you can rearrange most elements, complex types, and attributes by dragging-and-dropping. procedure open an xml profile. on the data elements or types tab, select an element, complex type or attribute. drag it to a new location in the tree. complex types on the types tab cannot be moved. if the new location is valid, a yellow arrow icon, dashed line, and yellow highlighting appear at the location and you can drop it at this location. if the element, complex type or attribute can become a parent or child, a green plus sign , dashed line, and yellow highlighting appear at the location and you can drop it at this location. if the new location is invalid, a red x appears — you cannot drop it at this location. click save.