these sample javascript or groovy custom scripting functions map the first three characters of the location code as the region id and the last five characters as the postal code. javascript example inputs locationcode (character) outputs regionid postalcode script region = locationcode.substring(0, 3); postalcode = locationcode.substring(locationcode.length-5, locationcode.length); groovy example inputs locationcode (character) outputs regionid postalcode script region = locationcode.substring(0, 3); postalcode = locationcode.substring(locationcode.length()-5, locationcode.length());