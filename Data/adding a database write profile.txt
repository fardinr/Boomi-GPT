for write profiles, the data elements tab is where you define the sql statement(s) to insert, update, or delete records. use the data elements tab to select the data for the database write for mapping data. select input fields based on requirements. database write profiles use input fields as the destination profile in a map.procedurecreate a database profile.on the options tab, set the execution type field to write.on the data elements tab click the statement node.do one of the following:set the type field to standard insert/update/date and enter or cut and paste your sql statement in the sql script field or the code editor. to open the code editor, click edit.set the type field to stored procedure write and in the stored procedure field, enter the stored procedure's name.set the type field to dynamic insert, dynamic update or dynamic delete and enter a table name.optional: if you set the type field to one of the dynamic options, you can click import to launch the import wizard to automatically generate the statement and input fields.on the data elements tab, add fields as needed.only dynamic delete profiles do not contain fields.click the drop-down arrow next to fields and choose add field.select the field in the left pane and in the right pane, change the field’s name.turn on the mandatory and/or the enforce unique check boxes, as needed.expand the data format options section and select a data type.repeat these steps as needed to add more fields.note: each question mark defined in the sql statement serves as a placeholder and must bind positionally to the field listed in the profile.optional: dynamic update and dynamic delete profiles can contain conditions. add conditions as needed.click the drop-down arrow next to conditions and choose add condition. select the condition in the left pane and in the right pane, change the condition’s name.select an operator.expand the data format options section and select a data type.repeat these steps as needed to add more conditions.click save.