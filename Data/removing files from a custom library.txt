you can remove custom jar files from a custom library component, but you should not do so if the component is currently deployed. procedure open the custom library component. pause on the name of a jar file and click . the file is removed from the included jar files list and returned to the selection list. after you select all of the jar files that you want to remove from the custom library component, click save.