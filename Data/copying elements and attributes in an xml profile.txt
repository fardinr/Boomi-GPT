you can copy elements and attributes from the data elements or types tabs. procedure create or open an xml profile and go to the data elements tab or the types tab. click the blue arrow next to an element or attribute, then select copy element or copy attribute. click the blue arrow next to a destination element, then select paste. the destination element can be on the same tab, on a different tab, or in a different xml profile. if the destination element is read-only, the paste option does not appear. the child element or attribute appears in the tree. all of the copied element and/or attribute’s properties and settings are copied. if the copied element has child elements or attributes, they are copied as well. the copied elements and/or attributes become children of the destination element. optional: to paste the element or attribute to another location, repeat the previous step. click save.